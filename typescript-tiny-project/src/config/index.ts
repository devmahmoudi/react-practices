import type { AppConfig } from "../types";

/**
 * App config
 */
const appConfig: AppConfig = {
  heading: {
    title: "Typescript & React Tutorial",
    subTitle: "Mahdi Mahmoudi",
  },
};

export default appConfig;